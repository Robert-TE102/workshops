//arrays

//we use arrays to store multiple values at a time

//structure

// let arrayName = ["value", "value", "value"]

// examples

let myFavouriteFood = ["burritos", "pizza", "sushi"];

let myFavouriteThings = ["sun", 46, true "sea"]
[No.][bulleon]
//array properties
let array = []

//index --> a number that indicates the position of a specific item in an array, starting with zero/0
// - in myFavouriteFood, "burritos" is index 0, in myFavouriteThings 46 is index 1.
syntax - myFavouriteThings[0];
myFavouriteThings[1];
console.log(myFavouriteThings[3]);

(node app.js) in terminal, allows you to run JS without an index / browser.

//length DOESN't start at zero/0.
// length --> a number that tells us the total amount of items in an array, the length of myFavouriteFood is 3.
myFavouriteFood.length; // 3
console.log (myFavouriteFood.length);
myfavouriteThings.length; // 4
console.log (myFavouriteThings.length);
indicates the number of things inside an array.

variable [let or const] - store one variable at a time.
